auto Help{LR"(Creates one or more directories.
Intermediate directories in path will also be created.

mkd <Path> [+ <Path>] [+ ...]

  Path - Relative or absolute directory path.

Examples:
  mkd directory ...
  mkd path/to/directory ...)"};